<div class="produit-item text-center">
         <a href="{{path('produit',{'slug': produit.slug})}}"> <img src="uploads/{{produit.illustration}}" alt="{{produit.name}}" class="img-fluid"></a> 
        <h5> {{produit.name}}</h5>
        <span class="produit-subtitle">{{produit.subtitle}}</span> <br>
        <span class="produit-price"> {{(produit.priceQuantity / 100)|number_format(2, ',', '.') }} €</span>
  </div>