<div class="produit-item text-center text-light">
         <a href="{{path('produit',{'slug': produit.slug})}}"> <img src="uploads/{{produit.illustration}}" alt="{{produit.name}}" class="img-fluid imgSize"  ></a> 
        <h5 class="mt-3"> {{produit.name}}</h5>
        <span class="produit-subtitle">{{produit.subtitle}}gr</span> <br>
        <span class="produit-price"> {{(produit.priceQuantity / 100)|number_format(2, ',', '.') }} €</span>
  </div>